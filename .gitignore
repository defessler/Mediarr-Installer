nas/.env
nas/.env.local

# Electron installer
installer/node_modules/
installer/out/
installer/dist/
installer/resources/nas-payload/
installer/.vite/

# Docs site (site/_site is generated by site/build.mjs on every build,
# and by CI on every deploy — committing it would mean reviewing a
# thousand lines of generated HTML on each content edit)
site/node_modules/
site/_site/

# OS clutter
.DS_Store
Thumbs.db

# Editor scratch
.claude/

# Python bytecode (from local py_compile syntax checks)
__pycache__/
*.pyc
